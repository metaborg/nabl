module pp

imports

	libstratego-gpp
	runtime/refactoring/-
	pp/NameBindingLanguage2-parenthesize
	pp/NameBindingLanguage2-pp

rules
	
  pp-NameBindingLanguage2-string =
    parenthesize-NameBindingLanguage2
    ; prettyprint-SDF-start-symbols
    ; !V([], <id>)
    ; box2text-string(|120)
      
  pp-partial-NameBindingLanguage2-string =
    parenthesize-NameBindingLanguage2
    ; prettyprint-example
    ; !V([], <id>)
    ; box2text-string(|120)
      
  pp-debug :
    ast -> result
    with
       result := <pp-NameBindingLanguage2-string> ast
    <+ <bottomup(try(not(is-string); not(is-list); not(pp-NameBindingLanguage2-string); debug(!"cannot pp ")))> ast
    ;  result := ""

rules
  
  /**
  * Given an old AST and a new AST, pretty-prints the new AST while presevering 
  * layout (e.g. whitespacing and comments) associated with the old AST.
  *
  * Example:
  *   new-text := <construct-textual-change> (old-ast, new-ast)
  */
  construct-textual-change = construct-textual-change(pp-partial-NameBindingLanguage2-string, parenthesize, override-reconstruction, resugar)


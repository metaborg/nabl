module generation/visits

imports

  libstrc
  src-gen/signatures/-

rules
  
  visit-to-appl:
    Visit(v, Args(arg*)) -> |[<s> ~v ]|
    with
      scope* := <map(?Arg(_, <id>))> arg*;
      s := |[ nabl-ts-traverse(|constraints,[~scope*]) ]|
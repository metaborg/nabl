module generation/visits

imports

  libstrc
  signatures/-

rules
  
  visit-to-appl:
    Scope(scope*, t) -> |[<s> ~t ]|
    with
      s := |[ nabl2-traverse(|facts__, constraints__, [~scope*], position__) ]|

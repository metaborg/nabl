module desugar

imports

  signatures/-

rules

  desugar-all =
    desugar-rules

  desugar-rules = try(sometd(desugar-rule))
  desugar-rule: Rule(h) -> Rule(h, NoLocalVars(), CTrue(), NoLocalVars(), CTrue())
  desugar-rule: Rule(h, vs, b) -> Rule(h, NoLocalVars(), CTrue(), vs, b)

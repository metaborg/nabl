module test/names

namespaces Foo Method Type Package

properties

modifiers of Method: List(Modifier)
parameter-types of Method: List(Type)

binding rules

// Invoke(Method(m), _):
//   filters Method m
//      with m has modifiers mod
//       and enclosing Type of Method m => mt
//       and enclosing Package of Method m => mp
//       and  
//         ( mod == Public()
//        or mod == Private()   and mt == ct
//        or mod == Default()   and mp == cp
//        or mod == Protected() and mp == cp 
//        or mod == Protected() and ct <: mt )
//     where enclosing Type of current Method scope => ct
//       and enclosing Package of current Method scope => cp      
 
Invoke(Method(m), a*):
  disambiguates Method m
    
    with m has modifiers mod
     and m has parameter-types pt*
     and enclosing Type of Method m => mt
     and enclosing Package of Method m => mp
     and  
       ( mod == Public()
      or mod == Private()   and mt == ct
      or mod == Default()   and mp == cp
      or mod == Protected() and mp == cp 
      or mod == Protected() and ct <: mt )
    
    by minimal distance at* <: pt* 
    
    where enclosing Type of current Method scope => ct
      and enclosing Package of current Method scope => cp      
      and a* has type at*
    
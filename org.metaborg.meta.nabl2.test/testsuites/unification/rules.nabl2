module testsuites/unification/rules

imports

  signatures/testsuites/-

rules

  [[ CannotUnifyRecursive() ^ () ]] :=
    ty == (ty,ty).

  [[ NestedTailsAreNil() ^ () ]] :=
    [|[|[]]] == [].

  [[ DoNotInferLocked() ^ () ]] :=
    .ty == ().